
async def search_viator_activities(
    query: str,
    location: str,
    checkin: str = None,
    checkout: str = None,
    limit: int = 6
):
    # Always return empty safely
    return []
